# Переводы строк — всегда LF, даже если git в Windows настроен на CRLF:
# иначе скрипты проверок ломаются в Git Bash и WSL.
* text=auto eol=lf
# Данные — байт в байт как выгрузили: BOM, CRLF и кодировку не трогать.
data/** -text
